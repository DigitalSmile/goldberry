/// `docs/core-widgets.md` §10's `timeline` — an ordered list of events along an
/// axis (ADR-0345).
///
/// [io.github.digitalsmile.goldberry.widgets.panel.timeline.Timeline] is the
/// list and [io.github.digitalsmile.goldberry.widgets.panel.timeline.Entry] is
/// one event on it. The rest is parts: the rail, the marker, the line and the
/// words, CSS-selectable and not constructible.
@NullMarked
package io.github.digitalsmile.goldberry.widgets.panel.timeline;

import org.jspecify.annotations.NullMarked;
