/// `docs/core-widgets.md` §10's `calendar`, and §4's `date-picker` popover
/// content.
///
/// `@NullMarked`, which puts this package under NullAway. See
/// `io.github.digitalsmile.goldberry.widgets.form.parts` for why every package
/// opts in one at a time.
@NullMarked
package io.github.digitalsmile.goldberry.widgets.panel.calendar;

import org.jspecify.annotations.NullMarked;
