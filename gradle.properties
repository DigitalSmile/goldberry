# The release line being worked towards, as YEAR.RELEASE[.PATCH] (ADR-0333).
# Never a -SNAPSHOT: every build is a snapshot of this line unless the release
# workflow passes -Pgoldberry.release=true with a matching v<version> tag.
# Bump it right after tagging -- docs/releasing.md.
goldberryVersion=2026.1

org.gradle.parallel=true
org.gradle.caching=true
